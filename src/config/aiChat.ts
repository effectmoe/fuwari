/**
 * サイト内AIチャットの公開スイッチ（2026-09-05 非公開化）
 *
 * 🔴 false = 全動線を塞ぐ。ヘッダーボタン・トップの帯・モーダル・スクリプトの
 *    すべてが「HTMLに出力されない」。CSSで隠すのではなく、描画そのものを止める。
 *    → ページのソースを見ても存在が分からない。URL直打ちで開ける口も無い。
 *
 * 🔴 機能は消していない。mcp.effect.moe の /chat エンドポイントは生きており、
 *    curl で叩けば従来どおり応答する（社内での回答品質の詰めに使うため）。
 *
 * 公開を再開する時は、この1行を true に戻してビルドするだけでよい。
 * 呼び出し側（LandingLayout / index）を書き換える必要は無い。
 *
 * 非公開にした理由: 回答内容がまだ詰め切れておらず、一般公開に耐えないため。
 *   経緯: wiki/refactoring/systems/effect-moe-ai-chat-quality-2026-09.md
 */
export const AI_CHAT_ENABLED = false;
